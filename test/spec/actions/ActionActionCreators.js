'use strict';

describe('ActionActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/ActionActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
